// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "mark.cummins"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
